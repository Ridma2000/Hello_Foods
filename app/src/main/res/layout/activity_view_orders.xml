<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/white">

    <!-- Spinner to select an order -->
    <Spinner
        android:id="@+id/spinnerOrders"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:background="@drawable/spinner_background"
        android:layout_marginBottom="16dp"/>

    <!-- TextView to display the selected order details -->
    <TextView
        android:id="@+id/textViewOrderDetails"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Order Details will appear here"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:layout_below="@id/spinnerOrders"
        android:padding="8dp"
        android:background="@drawable/textview_background"
        android:layout_marginBottom="16dp"/>

    <!-- Button to confirm the selected order -->
    <Button
        android:id="@+id/buttonConfirmOrder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Confirm Order"
        android:textColor="@color/white"
        android:background="@drawable/button_background"
        android:layout_below="@id/textViewOrderDetails"
        android:layout_centerHorizontal="true"
        android:padding="12dp"
        android:layout_marginTop="16dp"/>

</RelativeLayout>
